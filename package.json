{
  "name": "novellia-pets",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "api": "npm run start:dev --workspace novellia-pets-api",
    "api:build": "npm run build --workspace novellia-pets-api",
    "api:test": "npm run test --workspace novellia-pets-api",
    "db:seed": "npm run db:seed --workspace novellia-pets-api",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  }
}
